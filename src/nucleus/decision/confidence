export class ConfidenceModel {
  combine(values: number[]): number {
    if (!values.length) return 0;
    const sum = values.reduce((acc, v) => acc + v, 0);
    return sum / values.length;
  }

  threshold(value: number, min: number): boolean {
    return value >= min;
  }
}
